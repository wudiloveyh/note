nmap

```bash
-sn 只做主机发现，不做端口扫描
-Pn 跳过主机发现，直接尝试端口扫描
-sS SYN扫描（半开扫描，常用且隐蔽）
-sT TCP扫描，不隐蔽
-sU UDP扫描，慢，用于检测UDP服务
-sV 版本探测，探测服务版本
-p 扫描端口
-p- 扫描全端口
-F 快速扫描端口，扫描常见的100个端口
-O 系统指纹识别，不太准确
-T 扫描速度，默认是3，4快速且稳定，5太快了容易被检测
--min-rate
--max-rate 和上面的参数配合限制每秒发送包的速度
-A 全面扫描，太慢了
```

